@model MWear.Models.City
@{
    ViewBag.Title = "EditCities";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";

    List<MWear.Models.State> states = ViewBag.States;
}

<!-- CONTENT WRAPPER -->
<div class="ec-content-wrapper">
    <div class="content">
        <div class="breadcrumb-wrapper d-flex align-items-center justify-content-between">
            <div>
                <h1>Add Category</h1>
                <p class="breadcrumbs">
                    <span><a href="@Url.Action("Index","Admin")">Home</a></span>
                    <span><i class="mdi mdi-chevron-right"></i></span>Cities
                </p>
            </div>
            <div>
                <a href="@Url.Action("Index","CitiesWithRates")" class="btn btn-primary">
                    View All
                </a>
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <div class="card card-default">
                    <div class="card-header card-header-border-bottom">
                        <h2>Add Cities</h2>
                    </div>

                    <div class="card-body">
                        <div class="row ec-vendor-uploads">
                            <div class="col-lg-12 col-sm-12">
                                <div class="ec-vendor-upload-detail">
                                    @*<form  class="row g-3">*@
                                    @using (Html.BeginForm("EditCities", "CitiesWithRates", FormMethod.Post, new { @enctype = "multipart/form-data", @class = "row g-3" }))
                                    {
                                        @Html.AntiForgeryToken()
                                        <div class="col-md-12">
                                            <label for="inputEmail4" class="form-label">Category name</label>
                                            <input type="hidden" value="@Model.Id" name="Id" />
                                            <input type="text" class="form-control slug-title" value="@Model.CityName" name="CityName" required>
                                        </div>
                                        <div class="col-md-12">
                                            <label class="form-label">Select Privince</label>
                                            <select id="Categories" name="StateId" class="form-select">
                                                <option>Select Parent Category</option>
                                                @foreach (var item in states)
                                                {
                                                    if (item.Id == Model.StateId)
                                                    {
                                                        <option value="@item.Id" selected>@item.StateName</option>
                                                    }
                                                    else
                                                    {
                                                        <option value="@item.Id">@item.StateName</option>
                                                    }

                                                }

                                            </select>
                                        </div>
                                        <div class="col-md-12">
                                            <label for="inputEmail4" class="form-label">Category name</label>
                                            <input type="text" class="form-control slug-title" value="@Model.Charges" name="Charges" required>
                                        </div>
                                        <div class="col-md-12">
                                            <label for="inputEmail4" class="form-label">Category name</label>
                                            <input type="text" class="form-control slug-title" value="@Model.MaxOrderAmount" name="MaxOrderAmount" required>
                                        </div>

                                        <div class="col-md-8 mb-25">
                                            <label class="form-label">Fixed Order Amount</label>
                                            <div class="form-checkbox-box">
                                                <div class="form-check form-check-inline">
                                                    @if (Model.IsFixedCharges == true)
                                                    {

                                                        <input type="checkbox" name="AvailableChechbox" checked>
                                                    }
                                                    else
                                                    {

                                                        <input type="checkbox" name="AvailableChechbox">
                                                    }
                                                    <label>Yes</label>
                                                </div>

                                            </div>
                                        </div>

                                        <div class="col-md-12">
                                            <button type="submit" class="btn btn-primary">Submit</button>
                                        </div>
                                    }
                                    @*</form>*@
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div> <!-- End Content -->
</div> <!-- End Content Wrapper -->
